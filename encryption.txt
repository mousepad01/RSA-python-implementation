se citeste mesajul de criptat din mfile.txt

se citesc pe rand n si e din public_key.txt

se converteste mesajul intr un sir de caractere , astfel:
00 + cod_ascii_de_1_cifra || 0 + cod_ascii_de_2_cifre || cod_ascii_de_3_cifre

sparg sirul de caractere corespunzator mesajului, astfel:
aleg dimensiunea unei bucati de sir, intre 20 si 30
sparg sirul in bucati de dimensiunea respectiva, sau de lungime mai mica daca lungimea sirului nu divide lungimea unei bucati
adaug fiecarei bucati obtinute cate o cifra nenula arbitrara de la 1 la 9 la inceput si sfarsit
fiecare bucata o convertesc din string in int si o retin intr o lista

criptez individual fiecare numar din lista precedenta, cu ajutorul formulei:
crypted_packages[i] = message_packages[i] ^ e % n 

rescriu fisierul cfile.txt, sub urmatoarea forma:
pe prima linie retin numarul de packages - 1
pe fiecare dintre urmatoarele linii, retin cate un package criptat